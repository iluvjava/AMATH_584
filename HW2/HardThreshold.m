function Threshold = HardThreshold(m, n, gamma)
%   Given the size of a data matrix, this is going to return a hard threshold
%   for the number of singular values, with the assuption that the noise is
%   gaussian and it's uniform throughout. 
    Tau = lambda(beta)*sqrt(n)*gamma;
    
    function Fxout = lambda(beta)
        
    end
    
    

end 